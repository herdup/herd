%h1 Listing posts

%table
  %tr
    %th
    %th Title
    %th Body
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= herd_tag post.asset.n('index'), autoplay: true, loop:true, muted:true#, 'resize: 800x'
      %td= post.title
      %td= post.body
      %td= link_to 'Show', post
      %td= link_to 'Edit', edit_post_path(post)
      %td= button_to 'Destroy', post, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Post', new_post_path
